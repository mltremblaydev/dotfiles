# dotfiles

My personal shell config. Nothing fancy.

## What's in here

- `.zshrc` -- zsh config, prompt, aliases, the usual

## Usage

```zsh
ln -s ~/dotfiles/.zshrc ~/.zshrc
```

That's it. Not trying to reinvent anything here, just keeping my setup
portable between machines.
